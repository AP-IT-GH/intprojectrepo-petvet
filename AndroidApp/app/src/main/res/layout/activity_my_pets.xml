<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/addpetsbutton"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MyPetsActivity">
    <ListView
        android:id="@+id/list_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginBottom="8dp"
        android:onClick="LaunchAddPetActivity"
        android:text="Add pet"
        app:layout_constraintBottom_toBottomOf="@+id/list_view"
        app:layout_constraintEnd_toEndOf="@+id/list_view"
        app:layout_constraintStart_toStartOf="@+id/list_view"></Button>
</androidx.constraintlayout.widget.ConstraintLayout>